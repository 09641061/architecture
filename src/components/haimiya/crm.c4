model {

  // === Components > takodu.haimiya > CRM =====================================
  // Customer records with Decolecta-backed identity lookup.

  extend takodu.haimiya {
    crm = component 'CRM' {
      description '
        Customer records with Decolecta-backed
        identity lookup.
      '
      technology 'Spring Boot, Java 25'
    }
  }
}